<h1>Editing area</h1>

<% form_for(@area) do |f| %>
  <%= f.error_messages %>

<%= render :partial => 'form', :locals => {:f => f, :a => @area} %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @area %> |
<%= link_to 'Back', areas_path %>